import org.junit.Test;
import static org.junit.Assert.*;
import static org.mockito.Mockito.mock;

public class ListPanelTest {

    @Test
    public void testDoubleClickWithNonNullVar2AndNullCallbackAndEditOnDoubleClickTrue() {
        Callback doubleClickCallback = null;
        ListPanel listPanel = new ListPanel();
        Object var2 = new Object();
        listPanel.setEditOnDoubleClick(true);
        listPanel.setDoubleClickCallback(doubleClickCallback);

        listPanel.doubleClick(var2);

        // Verify that the editElement method is called with var2
        // assert...
    }

    @Test
    public void testDoubleClickWithNonNullVar2AndNullCallbackAndEditOnDoubleClickFalse() {
        ListPanel listPanel = new ListPanel();
        Object var2 = new Object();
        Callback doubleClickCallback = null;
        listPanel.setEditOnDoubleClick(false);
        listPanel.setDoubleClickCallback(doubleClickCallback);

        listPanel.doubleClick(var2);

        // Verify that neither the doubleClickCallback nor the editElement method is called
        // assert...
    }

    @Test
    public void testDoubleClickWithNullVar2() {
        ListPanel listPanel = new ListPanel();
        Object var2 = null;

        listPanel.doubleClick(var2);

        // Verify that neither the doubleClickCallback nor the editElement method is called
        // assert...
    }

    @Test
    public void testDoubleClickWithNonNullVar2AndNonNullCallback() {
        ListPanel listPanel = new ListPanel();
        Object var2 = new Object();
        Callback doubleClickCallback = new DeleteFileCallback();
        listPanel.setDoubleClickCallback(doubleClickCallback);

        SimpleChangeableListModel var1 = new SimpleChangeableListModel();
        listPanel.setListModel(var1);
        JList list = new JList(var1);
        listPanel.setList(list);

        listPanel.doubleClick(var2);

        // Verify that the callback method of DeleteFileCallback is called with var2
        // assert...
    }

    @Test
    public void testDoubleClickWithNonNullVar2AndNonNullCallback() {
        ListPanel listPanel = new ListPanel();
        Object var2 = new Object();
        Callback doubleClickCallback = new DeleteFileCallback();
        listPanel.setDoubleClickCallback(doubleClickCallback);

        String[] items = new String[]{"Apple", "Banana", "Orange", "Grapes", "Mango"};
        JList<String> itemList = new JList<>(items);
        itemList.setSelectionMode(ListSelectionModel.SINGLE_SELECTION);
        JList list = new JList(var1);
        listPanel.setList(list);

        listPanel.doubleClick(var2);

        // Verify that the callback method of DeleteFileCallback is called with var2
        // assert...
    }
}