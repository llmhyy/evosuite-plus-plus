package com.example;

public class function {
	public boolean function(int a,int b,double c,double d,double e,double f) {
		if(Math.abs(a)>Math.abs(b)) {
			if(Math.sin(c)<0) {
				if(Math.pow(a, 2)>10000) {
					if(Math.exp(d)>5000) {
						if(Math.ceil(e)<80) {
							if(Math.max(Math.log(f), Math.PI)==Math.PI) {
								c=d;
								if(Math.exp(c)>6000) {
									if(Util.checkF(b)>100) {
										return true;
									}
								}
							}
						}
					}
				}
			}
		}
		return false;
	}
}
